# Vamos aprender a enviar e-mail com o Python
# Esse é um processo de automação do envio dos e-mail

import smtplib # Importar a biblioteca smtplib
from email.mime.text import MIMEText  # biblioteca email

# conexão com os servidores do google
smtp_ssl_host = 'smtp.gmail.com' # Host do servivor
smtp_ssl_port = 465 # Porta do servidor
# username ou email para logar no servidor
username = 'seu_email@gmail.com' # Seu username
password = 'F6hvv58' # Digite aqui a sua senha, esse é apenas um exemplo

from_addr = 'seu_email@gmail.com' # E-mail do qual enviará a mensagem
to_addrs = ['e_mail_destinatario@gmail.com'] # E-mail do destinatário

# a biblioteca email possuí vários templates
# para diferentes formatos de mensagem
# neste caso usaremos MIMEText para enviar
# somente texto
message = MIMEText('Este é um e-mail com as informações sobre Notas Contábeis da sua empresa')
message['subject'] = 'Notas Contábis' # Insira o assunto do E-mail
message['from'] = from_addr
message['to'] = ', '.join(to_addrs)

# Conectaremos de forma segura usando SSL
server = smtplib.SMTP_SSL(smtp_ssl_host, smtp_ssl_port)
# para interagir com um servidor externo precisaremos
# fazer login nele
server.login(username, password)
server.sendmail(from_addr, to_addrs, message.as_string())
server.quit()
© 2021 GitHub, Inc

## Se após estas informações, o Python não mostrar erros, seu e-mail foi enviado com sucesso.
